
# WebSocket Broadcast Server

A minimal WebSocket server using Node.js and `ws` that broadcasts messages to all connected clients.

## Features

- Runs on port `8080`
- Sends a welcome message on connection
- Broadcasts any message to all connected WebSocket clients

## Run

```bash
node src/index.js
````

